---
description: Airbnb style - functions, arrow functions, parameters
globs: "**/*.ts"
alwaysApply: false
---

# Functions

## Arrow functions for callbacks

```typescript
// ✅ GOOD
items.map(item => item.id);
items.filter(item => item.active);

// ❌ BAD - function expressions for simple callbacks
items.map(function(item) { return item.id; });
```

## Implicit return for simple expressions

```typescript
// ✅ GOOD - implicit return
const double = (n: number) => n * 2;
const getName = (user: User) => user.name;

// ✅ GOOD - explicit return for complex logic
const process = (data: Data) => {
  const validated = validate(data);
  return transform(validated);
};
```

## Default parameters

```typescript
// ✅ GOOD
function createUser(name: string, role = 'user') {
  return { name, role };
}

// ❌ BAD - manual default handling
function createUser(name: string, role?: string) {
  role = role || 'user';
  return { name, role };
}
```

## Rest parameters over arguments

```typescript
// ✅ GOOD
function sum(...numbers: number[]) {
  return numbers.reduce((a, b) => a + b, 0);
}

// ❌ BAD - arguments object (not available in arrow functions)
function sum() {
  return Array.from(arguments).reduce((a, b) => a + b, 0);
}
```

## Never reassign parameters

```typescript
// ✅ GOOD
function process(input: string) {
  const sanitized = input.trim();
  return sanitized;
}

// ❌ BAD
function process(input: string) {
  input = input.trim();
  return input;
}
```
